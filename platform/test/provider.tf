/* -------------------------------------------------------------------------- */
provider "aws" {
  region  = "eu-west-1"
  assume_role {
    role_arn = "arn:aws:iam::767397883134:role/terraform-role"
  }
}
/* -------------------------------------------------------------------------- */
provider "helm" {
  kubernetes {
    config_path = "~/.kube/config"
  }
}
/* -------------------------------------------------------------------------- */
provider "kubernetes" {
  config_path = "~/.kube/config"
}
/* -------------------------------------------------------------------------- */
provider "time" {}
